name: apply
on:
  workflow_call:
    inputs:
      environment: { required: true, type: string }
      ref: { required: true, type: string }
      pr_number: { required: true, type: string }
      plan_run_id: { required: true, type: string }
      state_suffix: { required: true, type: string }
      # Absent on every apply dispatch (comment-ops only sends it for unlock),
      # so the default is what keeps those on the apply path.
      mode: { required: false, type: string, default: 'apply' }
    secrets:
      # `required: false` is load-bearing, not laxity: consumers hold this key
      # as a secret on the `shipmate-engine` environment (docs/github-app.md
      # §5), so a caller that binds no environment has no value to pass. It
      # does not need one — `summary` binds `environment: shipmate-engine`,
      # which resolves in the *caller's* repository, and an environment's value
      # wins over whatever the caller passed, empty included. A *required*
      # declaration would reject the call before any step of the first callee
      # job that binds no environment (`guard`), killing the run red with
      # "Secret ... is required, but not provided while calling".
      SHIPMATE_APP_PRIVATE_KEY: { required: false }
      SHIPMATE_PLAN_PASSPHRASE: { required: false }
jobs:
  # `gh workflow run apply.yml` by a human with write access would otherwise
  # skip every requirement in scripts/authorize. A human cannot make
  # github.actor a bot login, so this rejects any human dispatch -- though it
  # admits any bot actor with actions:write, not only this App.
  #
  # Its own job, not a first step of `detect`: `summary` must refuse a rejected
  # dispatch (it mints the App key and comments on the dispatcher-supplied PR
  # number) while still posting the failure comment and refreshing the gate
  # when `detect` genuinely fails -- an env_order cycle, a bad plan run id.
  # Keying `summary` on this job separates
  # the two: `needs.guard.result == 'success'` is false only for an
  # unauthorised dispatch. No secrets, no environment, no checkout.
  guard:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Reject dispatches that did not come from a bot actor
        if: ${{ !endsWith(github.actor, '[bot]') }}
        run: |
          echo "::error::apply must be dispatched by the shipmate App via comment-ops, not by a direct workflow_dispatch"
          exit 1

  # The review state this apply rests on, re-read here rather than trusted from
  # comment-ops: `guard` above admits any bot actor holding actions:write, so a
  # dispatch input would be a claim that surface could forge. Byte-identical to
  # `apply-all.yml`'s job -- one shape, one decision, on both apply paths.
  # Its own job because `detect` checks out the PR head and runs Terramate over
  # branch-controlled HCL -- no token belongs in that job. `environment:` is how
  # a callee reaches the App key (same mechanism as `summary` below).
  #
  # Deliberately unconditional -- no `if:`. A gate on the repository variable is
  # what let a `ungated-envs` action input wider than the variable escape
  # enforcement, and running always is also what closes the TOCTOU between the
  # comment that authorized this apply and the dispatch: the decision is re-read
  # here, so an approval dismissed in between holds. `apply-detect` refuses the
  # run on the decision; this job only fetches it.
  review:
    needs: [guard]
    runs-on: ubuntu-latest
    environment: shipmate-engine
    permissions: {}
    outputs:
      decision: ${{ steps.rd.outputs.decision }}
    steps:
      - name: Verify the App key arrived
        shell: bash
        env:
          APP_ID: ${{ vars.SHIPMATE_APP_ID }}
          PRIVATE_KEY: ${{ secrets.SHIPMATE_APP_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          if [ -z "$PRIVATE_KEY" ]; then
            echo "::error::The shipmate App private key did not reach this job. Three causes, in the order worth checking: (1) the calling workflow used 'secrets: inherit' across an organization boundary -- GitHub delivers no inherited secrets there, and inherit also suppresses the value this job's environment binding would otherwise supply, so pass the secret by name instead; (2) the caller left SHIPMATE_APP_PRIVATE_KEY out of its secrets block; (3) this job binds no environment, or binds one that holds no secret of that name. See docs/getting-started.md."
            exit 1
          fi
          [ -n "$APP_ID" ] || { echo "::error::SHIPMATE_APP_ID is unset. Set it as a repository or organization variable (docs/github-app.md)."; exit 1; }
      - id: token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.SHIPMATE_APP_ID }}
          private-key: ${{ secrets.SHIPMATE_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ github.event.repository.name }}
          permission-pull-requests: read
      - id: rd
        shell: bash
        env:
          GH_TOKEN: ${{ steps.token.outputs.token }}
          OWNER: ${{ github.repository_owner }}
          PR_NUMBER: ${{ inputs.pr_number }}
        run: |
          set -euo pipefail
          # NONE is the sentinel for a PR that exists with a null decision --
          # the ruleset requires no review. Nothing here has proved the PR
          # exists (unlike comment-ops, which reads it over REST first), and a
          # pr_number matching no pull request returns a null `pullRequest`
          # with no errors and exit 0 -- so that case gets its own value, which
          # detect's catch-all holds every env on. Never defaulted in a
          # workflow expression: a decision that fails to arrive must arrive
          # empty, which holds every env too.
          # The $owner/$repo/$pr tokens are GraphQL query variables (bound via
          # -F), not shell variables -- single quotes are intentional.
          # shellcheck disable=SC2016
          rd=$(gh api graphql -f query='query($owner:String!,$repo:String!,$pr:Int!){repository(owner:$owner,name:$repo){pullRequest(number:$pr){reviewDecision}}}' \
            -F owner="$OWNER" -F repo="${GITHUB_REPOSITORY#*/}" -F pr="$PR_NUMBER" \
            --jq '.data.repository.pullRequest | if type == "object" then (.reviewDecision // "NONE") else "MISSING_PR" end')
          echo "decision=$rd" >> "$GITHUB_OUTPUT"

  detect:
    needs: [guard, review]
    runs-on: ubuntu-latest
    permissions: { contents: read, checks: read, actions: read }
    outputs:
      waves: ${{ steps.d.outputs.waves }}
      # The unlock queue, and the literal `[]` in apply mode. Unlike `waves`
      # -- which is written in apply mode only, so it resolves to the empty
      # string in unlock mode and must never be fromJSON'd there.
      cells: ${{ steps.d.outputs.cells }}
      empty: ${{ steps.d.outputs.empty }}
      head_sha: ${{ steps.d.outputs.head_sha }}
      plan_run_id: ${{ steps.d.outputs.plan_run_id }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with: { fetch-depth: 0, ref: "${{ inputs.ref }}" }
      - uses: ship-iac/shipmate/actions/setup@21d05c37ca0199767059abfecd28242460b86a0c
        with:
          terramate-version: ${{ vars.TERRAMATE_VERSION }}
          tofu-version: ${{ vars.TOFU_VERSION }}
      - id: d
        uses: ship-iac/shipmate/actions/apply-detect@23ec9dbd5a3baecf8315f8c2f1fd7e68bd815d84
        with:
          environment: ${{ inputs.environment }}
          mode: ${{ inputs.mode }}
          plan-run-id: ${{ inputs.plan_run_id }}
          head-sha: ${{ inputs.ref }}
          github-token: ${{ github.token }}
          app-id: ${{ vars.SHIPMATE_APP_ID }}
          # Passed through raw, never defaulted: an empty decision means the
          # review signal never arrived, and empty refuses the run.
          ungated-envs: ${{ vars.SHIPMATE_UNGATED_ENVS }}
          review-decision: ${{ needs.review.outputs.decision }}
      # The apply path runs this pre-flight in apply-env-level's `snapshot` job,
      # before any wave binds an environment; the unlock job binds the SAME
      # `<env>-apply` and so needs the same refusal. GitHub creates a missing
      # environment on demand with no reviewers, no wait timer and no branch
      # policy -- and the created environment then satisfies the apply path's own
      # pre-flight, so an unlock into a missing environment would silently retire
      # the reviewer gate for every later apply of that env. A failure here fails
      # `detect`, which the unlock job's `!failure()` reads as "do not run".
      # The single flat queue is wrapped as one wave: the script's input shape is
      # the waves object, and it refuses an empty cell set by design -- hence the
      # `!= '[]'` clause rather than letting it decide.
      - name: Verify the unlock-path environment exists
        if: ${{ inputs.mode == 'unlock' && steps.d.outputs.cells != '[]' }}
        uses: ship-iac/shipmate/actions/verify-environments@9c0354a97cd97e07967dac2c7e36f524cdc18700
        with:
          waves-json: ${{ format('{{"wave0":{0}}}', steps.d.outputs.cells) }}
          shared-envs: ${{ vars.SHIPMATE_SHARED_ENVS }}
          github-token: ${{ github.token }}

  # A targeted apply is single-env by design, so its pending wave set is exactly
  # one env-level's wave matrix — run it through the same engine the deploy and
  # bare-apply paths use instead of duplicating wave0..wave7 here.
  apply:
    # Display name only (the job id stays `apply`): a called workflow's jobs
    # display as `<caller job> / <this name> / <wave job>`, and GHA cannot
    # suppress a level — so this level is named for what it fans out instead of
    # repeating the verb the wave job already carries.
    name: waves
    # `guard` and `review` are in `needs` as well as `detect`, so their failures
    # reach `!failure()` here: without them a rejected dispatch (or a review job
    # that could not mint the App key) skips `detect`, and a skipped `detect`
    # reads its outputs as empty strings -- which the `!= 'true'` guard below
    # reads as "not empty" and fans out anyway.
    needs: [guard, review, detect]
    if: ${{ !failure() && !cancelled() && inputs.mode != 'unlock' && needs.detect.outputs.empty != 'true' }}
    uses: ship-iac/shipmate/.github/workflows/apply-env-level.yml@8dcf08e1ed6bc14fbedfc532151f91f314ebb172 # pinned to a commit on main
    permissions: { contents: read, checks: read, actions: read, id-token: write }
    with:
      waves_json: ${{ needs.detect.outputs.waves }}
      head_sha: ${{ needs.detect.outputs.head_sha }}
      plan_run_id: ${{ needs.detect.outputs.plan_run_id }}
      state_suffix: ${{ inputs.state_suffix }}
    # Named, never `inherit`: these two files sit in one organization but the RUN
    # belongs to the consumer, and `inherit` is evaluated against the run -- so a
    # cross-organization consumer's `complete` job would hold no key, and its
    # applies would execute and strand their apply checks pending forever.
    # Ordering rule for adding a secret here: declare it in apply-env-level.yml,
    # merge, bump the pin below, and only then map it in this block. Mapping a
    # secret the pinned callee does not declare is a hard load-time error (unlike
    # an undeclared action input, which is silently ignored) -- docs/releasing.md.
    secrets:
      SHIPMATE_APP_PRIVATE_KEY: ${{ secrets.SHIPMATE_APP_PRIVATE_KEY }}
      SHIPMATE_PLAN_PASSPHRASE: ${{ secrets.SHIPMATE_PLAN_PASSPHRASE }}

  # Releasing a stranded lock is its own job, not a mode inside the wave jobs: an
  # `if:` on an `apply-cell` step is a seam whose deletion silently turns an
  # unlock into an apply. Nothing here is wave-ordered either -- releasing a lock
  # has no dependencies -- so the matrix is flat.
  #
  # The concurrency group is byte-identical to the wave jobs' (guarded by
  # scripts/tests/test_apply_cell_concurrency_guard.py), and that identity is the
  # whole point: a live apply for this cell makes the unlock queue behind it, so
  # by the time it runs the lock is either gone or genuinely orphaned. Do not
  # vary it by mode.
  #
  # No PR comment, deliberately -- do not "fix" this. Posting one needs
  # pull-requests: write, which the consumer wrapper does not grant to this
  # workflow, and widening a consumer's token to make a message prettier is the
  # wrong trade. Acceptance is already acknowledged by comment-ops' reaction,
  # per-cell detail lands in each job's step summary, and a failure reds the run
  # and notifies the actor.
  unlock:
    # `guard` and `review` are in `needs` for the same reason the waves job has
    # them: a rejected dispatch leaves `detect` skipped, and a skipped `detect`
    # reads its outputs as empty strings -- which `!= '[]'` reads as a queue.
    needs: [guard, review, detect]
    if: ${{ !failure() && !cancelled() && inputs.mode == 'unlock' && needs.detect.outputs.cells != '[]' }}
    runs-on: ubuntu-latest
    permissions: { contents: read, actions: read, id-token: write }
    strategy:
      # One cell that cannot determine its lock state must not stop its siblings
      # from releasing theirs.
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.detect.outputs.cells) }}
    environment: >-
      ${{ contains(format(',{0},', vars.SHIPMATE_SHARED_ENVS), format(',{0},', matrix.environment))
      && matrix.environment || format('{0}-apply', matrix.environment) }}
    concurrency:
      group: apply-${{ matrix.environment }}-${{ matrix.stack }}
      cancel-in-progress: false
    name: unlock / ${{ matrix.stack }} / ${{ matrix.environment }}
    env:
      TF_VAR_env: ${{ vars.TF_VAR_env }}
      TF_VAR_region: ${{ vars.TF_VAR_region }}
      TF_WORKSPACE: ${{ vars.TF_WORKSPACE }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with: { fetch-depth: 0, ref: "${{ inputs.ref }}" }
      - uses: ship-iac/shipmate/actions/setup@21d05c37ca0199767059abfecd28242460b86a0c
        with:
          terramate-version: ${{ vars.TERRAMATE_VERSION }}
          tofu-version: ${{ vars.TOFU_VERSION }}
      # A consumer opts into AWS OIDC per environment (plan/apply roles may
      # differ per env/account), and AWS_ROLE_ARN_<WORKLOAD> wins over
      # AWS_ROLE_ARN so one environment can serve several workloads.
      - name: Configure cloud credentials (AWS OIDC)
        if: ${{ (matrix.workload_var != '' && vars[format('AWS_ROLE_ARN_{0}', matrix.workload_var)]) != '' || vars.AWS_ROLE_ARN != '' }}
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          role-to-assume: ${{ matrix.workload_var != '' && vars[format('AWS_ROLE_ARN_{0}', matrix.workload_var)] || vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}
      - uses: ship-iac/shipmate/actions/unlock-cell@e370ae88a3fa8f174b6f51e43967ce2cdf0eacdd
        with:
          stack: ${{ matrix.stack }}
          stack-name: ${{ matrix.stack }}
          env: ${{ matrix.environment }}

  summary:
    # `review` is in this list because `results:` below is
    # `join(needs.*.result, ',')` -- which covers only THIS job's needs. A
    # failed `review` skips `detect` (correctly: nothing applies, the gate stays
    # pending) while `summary` still runs on `guard`'s success, so without the
    # entry the comment renders the non-failure form over a run that died.
    needs: [guard, review, detect, apply]
    # `always()` alone would still run this job -- and mint the App key -- when
    # the bot-actor rejection failed; `needs.guard.result == 'success'` closes
    # that. Keyed on `guard` and NOT on `detect`: `always()` is needed so
    # results/gate-refresh post when apply failed or was cancelled, and a
    # genuine `detect` failure (a bad plan run id) must still get the developer
    # a failure comment and a gate refresh.
    if: ${{ always() && needs.guard.result == 'success' }}
    runs-on: ubuntu-latest
    # Both steps below mint the App key (via secrets.SHIPMATE_APP_PRIVATE_KEY),
    # same as apply-env-level.yml's complete job -- deployment branch policy
    # restricts this environment to the default branch, so a branch-authored
    # workflow naming it is denied and never sees the key.
    environment: shipmate-engine
    # gate-refresh mints its own App token (checks:read scan + statuses:write
    # gate write); apply-summary mints its own App token too, also
    # checks:read now -- it scans the head SHA's apply checks to fold their
    # state into the result comment, not just the comment POST. actions:read
    # is needed here for apply-summary's artifact download and run-jobs
    # listing.
    permissions: { actions: read }
    steps:
      # Both steps skip for an unlock: it completes no apply check, so a gate
      # refresh could only be a no-op, and an apply-result comment over an
      # unlock run has no cells to report and would mislead.
      - name: Complete gate if every apply check is done
        if: ${{ inputs.mode != 'unlock' }}
        id: gate
        uses: ship-iac/shipmate/actions/gate-refresh@4ab4d7a13c1db20003938dbd72b34290217176f4
        with:
          head-sha: ${{ inputs.ref }}
          app-id: ${{ vars.SHIPMATE_APP_ID }}
          private-key: ${{ secrets.SHIPMATE_APP_PRIVATE_KEY }}
      - name: Post apply result comment
        if: ${{ inputs.mode != 'unlock' }}
        uses: ship-iac/shipmate/actions/apply-summary@23ec9dbd5a3baecf8315f8c2f1fd7e68bd815d84
        with:
          pr-number: ${{ inputs.pr_number }}
          head-sha: ${{ inputs.ref }}
          environment: ${{ inputs.environment }}
          waves-json: ${{ needs.detect.outputs.waves }}
          results: ${{ join(needs.*.result, ',') }}
          gate-verdict: ${{ steps.gate.outputs.verdict }}
          github-token: ${{ github.token }}
          app-id: ${{ vars.SHIPMATE_APP_ID }}
          private-key: ${{ secrets.SHIPMATE_APP_PRIVATE_KEY }}
