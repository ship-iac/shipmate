name: unlock
on:
  workflow_call:
    inputs:
      environment: { required: true, type: string }
      ref: { required: true, type: string }
# Floor, not a default to inherit from: every job here declares its own block, so a job that
# loses its block gets nothing rather than everything the caller granted, id-token: write included.
permissions: {}
jobs:
  # `gh workflow run unlock.yml` by a human with write access would otherwise skip every
  # requirement in scripts/authorize. A human cannot make github.actor a bot login, so this rejects
  # any human dispatch -- though it admits any bot actor with actions:write, not only this App.
  guard:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Reject dispatches that did not come from a bot actor
        if: ${{ !endsWith(github.actor, '[bot]') }}
        run: |
          echo "::error::unlock must be dispatched by the shipmate App via comment-ops, not by a direct workflow_dispatch"
          exit 1

  detect:
    needs: [guard]
    runs-on: ubuntu-latest
    permissions: { contents: read, checks: read, actions: read }
    outputs:
      cells: ${{ steps.d.outputs.cells }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with: { fetch-depth: 0, ref: "${{ inputs.ref }}" }
      - uses: ship-iac/shipmate/actions/setup@21d05c37ca0199767059abfecd28242460b86a0c
        with:
          terramate-version: ${{ vars.TERRAMATE_VERSION }}
          tofu-version: ${{ vars.TOFU_VERSION }}
      - id: d
        uses: ship-iac/shipmate/actions/apply-detect@a9be68cc9442bbf03478a7eabbd0afc2847cdd4e
        with:
          environment: ${{ inputs.environment }}
          # A literal, not an input: this file is the verb. `run_unlock` reads
          # neither the review decision nor the ungated-env list -- an approval
          # reviews a diff and unlock applies none -- so neither is passed.
          mode: unlock
          head-sha: ${{ inputs.ref }}
          github-token: ${{ github.token }}
          app-id: ${{ vars.SHIPMATE_APP_ID }}
      # The apply path runs this pre-flight in apply-env-level.yml's `snapshot` job, before any
      # wave binds an environment; the `unlock` job binds the same `<env>-apply`, so it needs the
      # same refusal. A failure here fails `detect`, which `unlock`'s `!failure()` reads as "do
      # not run".
      - name: Verify the unlock-path environment exists
        if: ${{ steps.d.outputs.cells != '[]' }}
        # GitHub creates a missing environment on demand with no reviewers, no wait timer and no
        # branch policy, and that created environment then satisfies the apply path's own
        # pre-flight -- so an unlock into a missing one silently retires that env's reviewer gate.
        uses: ship-iac/shipmate/actions/verify-environments@a9be68cc9442bbf03478a7eabbd0afc2847cdd4e
        with:
          # The flat queue is wrapped as one wave because the action's input shape is the waves
          # object. It refuses an empty cell set by design, which is why the step's `if:` filters
          # `[]` out rather than letting it decide.
          waves-json: ${{ format('{{"wave0":{0}}}', steps.d.outputs.cells) }}
          shared-envs: ${{ vars.SHIPMATE_SHARED_ENVS }}
          github-token: ${{ github.token }}

  unlock:
    # `guard` is in `needs` as well as `detect`: a rejected dispatch leaves `detect` skipped, and a
    # skipped `detect` reads its outputs as empty strings -- which `!= '[]'` reads as a queue.
    needs: [guard, detect]
    if: ${{ !failure() && !cancelled() && needs.detect.outputs.cells != '[]' }}
    runs-on: ubuntu-latest
    # No PR comment, deliberately -- do not "fix" it. Posting one needs pull-requests: write, which
    # the consumer wrapper does not grant this workflow, and widening a consumer's token for a
    # prettier message is the wrong trade. comment-ops' reaction acknowledges acceptance, per-cell
    # detail lands in each job's step summary, and a failure reds the run and notifies the actor.
    permissions: { contents: read, actions: read, id-token: write }
    strategy:
      # One cell that cannot determine its lock state must not stop its siblings from releasing
      # theirs.
      fail-fast: false
      # Nothing here is wave-ordered -- releasing a lock has no dependencies -- so the matrix is
      # flat.
      matrix:
        include: ${{ fromJSON(needs.detect.outputs.cells) }}
    environment: >-
      ${{ contains(format(',{0},', vars.SHIPMATE_SHARED_ENVS), format(',{0},', matrix.environment))
      && matrix.environment || format('{0}-apply', matrix.environment) }}
    # Byte-identical to the wave jobs' group in apply-env-level.yml (guarded by
    # scripts/tests/test_apply_cell_concurrency_guard.py and test_verb_path_isolation.py). That
    # identity queues an unlock behind a live apply for the cell, so the lock is then gone or
    # genuinely orphaned by the time the unlock runs.
    concurrency:
      group: apply-${{ matrix.environment }}-${{ matrix.stack }}
      cancel-in-progress: false
    name: unlock / ${{ matrix.stack }} / ${{ matrix.environment }}
    env:
      TF_VAR_env: ${{ vars.TF_VAR_env }}
      TF_VAR_region: ${{ vars.TF_VAR_region }}
      TF_WORKSPACE: ${{ vars.TF_WORKSPACE }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with: { fetch-depth: 0, ref: "${{ inputs.ref }}" }
      - uses: ship-iac/shipmate/actions/setup@21d05c37ca0199767059abfecd28242460b86a0c
        with:
          terramate-version: ${{ vars.TERRAMATE_VERSION }}
          tofu-version: ${{ vars.TOFU_VERSION }}
      # A consumer opts into AWS OIDC per environment (plan/apply roles may
      # differ per env/account), and AWS_ROLE_ARN_<WORKLOAD> wins over
      # AWS_ROLE_ARN so one environment can serve several workloads.
      - name: Configure cloud credentials (AWS OIDC)
        if: ${{ (matrix.workload_var != '' && vars[format('AWS_ROLE_ARN_{0}', matrix.workload_var)]) != '' || vars.AWS_ROLE_ARN != '' }}
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          role-to-assume: ${{ matrix.workload_var != '' && vars[format('AWS_ROLE_ARN_{0}', matrix.workload_var)] || vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}
      - uses: ship-iac/shipmate/actions/unlock-cell@a9be68cc9442bbf03478a7eabbd0afc2847cdd4e
        with:
          stack: ${{ matrix.stack }}
          stack-name: ${{ matrix.stack }}
          env: ${{ matrix.environment }}
